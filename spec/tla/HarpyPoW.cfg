\* Safe configuration: the adversary's total budget cannot exceed the
\* confirmation depth, so k-deep commits must hold. Expect: no error.
CONSTANTS
  MaxHonestBlocks = 4
  MaxAdvBlocks = 3
  ConfirmDepth = 3
SPECIFICATION Spec
INVARIANTS
  TypeOK
  CommittedStable
CHECK_DEADLOCK FALSE
