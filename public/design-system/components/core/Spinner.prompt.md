Circular loader (ink track, amber head). Pair with terse copy: "Mining…".

```jsx
<Spinner size={16} />
<span style={{ display: "inline-flex", alignItems: "center", gap: 8 }}>
  <Spinner /> Mining…
</span>
```
