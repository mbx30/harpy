Square icon-only button for toolbars and card corners.

```jsx
<IconButton aria-label="Copy hash"><Icon name="copy" /></IconButton>
<IconButton variant="outline" aria-label="Settings"><Icon name="settings" /></IconButton>
```

- `ghost` (default) or `outline`; sizes sm/md/lg (28/36/44px).
- Always set `aria-label`.
