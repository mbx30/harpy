The standard Harpy container — white, hairline border, 10px radius, `--shadow-sm`, 20px padding.

```jsx
<Card>
  <h3 style={{ margin: 0, fontSize: "var(--text-md)", fontWeight: 600 }}>Block #12</h3>
  <p style={{ color: "var(--text-secondary)" }}>Mined 2 minutes ago</p>
</Card>
<Card interactive onClick={open}>…</Card>
<Card variant="sunken">…</Card>  {/* wells, code areas */}
```
