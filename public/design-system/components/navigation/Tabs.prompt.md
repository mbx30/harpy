Underline tabs with an amber active indicator. Works controlled or uncontrolled.

```jsx
<Tabs
  tabs={[{ value: "chain", label: "Chain" }, { value: "api", label: "API" }]}
  defaultValue="chain"
  onChange={setView}
/>
```
